import react from "react"

export default function EditPen({
        id,
    label,
    name,
    value,
    placeholder,
    readonly,
    onChange,
    onToggle,
    type = "text",
    error,
    hidePencil,
}) {
    return (
        <div className="profile-form-container">'
        </div>
    )
}